#!/usr/bin/env -S zsh -i
# This script runs the specified binary in an interactive zsh session (so that dotfiles are loaded),
# forwarding all command-line arguments to the binary in an exact manner.

$@
